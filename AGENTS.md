# AGENTS.md

## 仓库结构说明

本目录（`typescript_projects`）采用 **monorepo** 结构管理所有 TypeScript 项目：

- 每个子目录都是一个独立的项目（例如 `taskmon`），彼此互不依赖归属关系，各自维护自己的依赖与配置。
- 整个目录作为单一 Git 仓库进行版本管理，不再为单个项目单独建仓。
- 根目录**不创建** `pnpm-workspace.yaml` 和根 `package.json`，子项目之间不做 workspace 关联。
- 新增项目时直接在目录下创建新的子目录即可，不要在子项目内单独 `git init`。
- 在本目录下工作时，先确认目标项目所在子目录，再在该子目录的上下文中执行构建、测试等操作（如 `pnpm install`、`pnpm dev`、`pnpm build`）。

## 隔离性原则（重要）

- 各子目录（子项目）之间**基本上无任何关联**：没有共享代码、共享依赖或隐含约定，不要假设对一个项目的改动需要另一个项目"配合"。
- 开发某个子目录时，**只在该子目录范围内工作**，不要想着顺带修改另一个子目录的事情。
- 即使认为其他子目录似乎也需要相应改动，也不要自行跨项目修改；先向用户说明情况，由用户决定是否另行处理。
