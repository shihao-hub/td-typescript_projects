{
  "name": "cli-json-reel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CLI + JSON 输出模式：极简排版动画演示视频（纯 Canvas2D，确定性离线渲染）",
  "scripts": {
    "render": "node scripts/render.mjs",
    "verify": "node scripts/render.mjs --verify --limit 6 --no-encode --keep-frames",
    "preview": "node scripts/render.mjs --preview"
  },
  "devDependencies": {
    "playwright-core": "^1.49.0"
  }
}
